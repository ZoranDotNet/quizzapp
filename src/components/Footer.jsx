import React from "react";

const Footer = () => {
  return (
    <div className="footer">
      <span>Keep scrolling, the fun doesn't stop here 🩶</span>
    </div>
  );
};

export default Footer;
